---
id: "{task_prefix}_{NN}_{type}_{short_slug}" # 生成规则见 SKILL.md「命名规则（唯一定义处）」：文件名去扩展名、加 $TASK_PREFIX，连字符转下划线。含 {}、| 等特殊字符的值必须加引号，保证 front matter 是合法 YAML
name: "{任务显示名称}"
type: "{documentation|data_model|migration|persistence|contract|domain_logic|service|api|ui|integration|test|observability}" # 项目自有任务类型体系优先；或通用 type + subtype 组合
subtype: null # 可选：项目自有任务类型；沿用项目既有 tasks/ 文档体系时填写
status: pending # pending | in_progress | completed | blocked
blocked_reason: null # 仅 blocked 状态填写：缺口类别（依赖/结构/文档/环境/权限/用户决策）+ 描述 + 解除条件
depends: [] # 依赖任务 ID；引用的任务必须已存在；completed 任务禁止追加依赖
profiles: [] # 项目探测确认的 profile
files: [] # 确认过的现有或新增文件路径
---

# {任务显示名称}

## 任务范围

| 项目 | 内容 |
|------|------|
| 类型 | `{任务类型}`（`subtype` 如有） |
| 已启用 profile | `{项目探测确认的 profile}` |
| 架构边界 | `{实际模块、层级、feature slice 或目录}` |
| 结构分析 | `{索引工具查询与结论；或只读降级限制}` |
| 完成条件 | `{实现、文档和验证条件}` |

## 需求与验收

- 用户目标：{目标}
- 包含：{本任务承担的行为}
- 不包含：{明确不做的内容}
- 验收：{可观察结果、错误/边界条件、兼容要求}

## 设计与依赖

| 类别 | 事实 / 决策 |
|------|-------------|
| 输入与输出 | {UI 行为、API/event contract、类型或数据变化} |
| 数据/Schema | {表、模型、数据变更策略；无则“不适用”} |
| 集成 | {外部系统/SDK/消息/认证；无则“不适用”} |
| 安全与质量 | {权限、审计、性能、幂等、观测} |
| 依赖 | {前置任务、调用方、兼容或发布顺序} |

## 实现步骤

1. {遵循项目现有模式的步骤}
2. {实现或修改的代码/配置/文档}
3. {异常、兼容、发布或迁移处理}

## 涉及文件

| 文件 | 模块/区域 | 操作 | 说明 |
|------|-----------|------|------|
| {路径} | {实际架构区域} | 新增/修改 | {说明} |

## 完整代码（供手动敲写）

按「涉及文件」逐个文件给出**可直接敲写的完整代码**——用户可能手动实现本任务，代码必须完整到无需猜测：

- 新增文件：给全量代码，可直接落盘。
- 修改文件：给出修改后所在类/方法/代码块的完整内容，并在说明中写清精确落点（类名、方法名、相对位置），不写碎片 diff。
- 代码依据已确认的设计文档、项目规范与相邻实现编写；禁止 `// TODO`、`return null;`、`...省略...` 等占位伪代码。
- 依赖未完成或任务 `blocked` 暂时无法给出完整代码时，在此说明原因，条件具备后回填本节。
- 执行中回写代码时，在代码块末尾追加修订记录行（`修订：N - 原因`），保留最终版代码；重大偏差同步更新设计文档。

<!-- 每个涉及文件复制一个 ### 小节；小节标题标注 新增/修改 -->

### {文件路径}（新增）

```{语言标识}
{完整代码}
```

### {文件路径}（修改）

```{语言标识}
{修改后所在类/方法的完整代码块}
```

修改点说明：{改了什么、为什么；落点：类名/方法名/相对位置}

## 验证

| 验证方式 | 命令或步骤 | 预期结果 |
|----------|------------|----------|
| {lint/test/build/manual} | {项目实际命令或手动步骤} | {通过条件} |

## 风险与阻塞

- 风险：{数据、权限、兼容、性能或外部依赖风险}
- 阻塞：{无 / blocked 原因与需要的用户决策}；解除后在此追加：`[unblock] 日期：决策人=用户，决策=<内容>`
